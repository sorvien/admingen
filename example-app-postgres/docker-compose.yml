services:
  postgres:
    image: postgres:16-alpine
    container_name: admingen-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: admingen_advanced
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: